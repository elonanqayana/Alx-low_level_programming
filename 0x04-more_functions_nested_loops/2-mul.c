#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply
 * Return: The result of muliply x param and y param
 */
int mul(int x, int y)
{
	return(x * y);
}
